#LIS
n = int(input())
array = list(map(int,input().split()))
array.reverse() #LIS를 역순으로 출력하는 프로그램

dp = [1]*n #dp테이블을 만들어주고

for i in range(1,n):  #i가 n번 반복될떄
    for j in range(0,i):  #0부터 i번째까지 확인을 해준다
        if array[j] <array[i]:  #i가 j보다 클 때
            dp[i] = max(dp[i],dp[j]+1)  #dp테이블의 i번째에다가 더 큰값을 더하면서 넣어준다 
print(n-max(dp))
